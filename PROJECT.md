# WeChatBuddy 项目说明

## 项目目标

开发一个支持 Apple Silicon 的 macOS 菜单栏应用。用户在微信输入框中按下 `Command + Shift + R` 后，应用读取草稿，通过 OpenAI API 完成错别字纠正、表达润色、自然化和场景语气优化，再把结果写回原输入框。消息始终由用户检查并手动发送。

## 技术架构

采用分层、协议驱动的结构，隔离系统交互与网络服务，方便测试和替换实现。

### 表现层

- `App`：SwiftUI 应用入口与生命周期。
- `MenuBar`：状态、权限入口、设置和错误提示。
- `Settings`：API Key、模型、快捷键及语气偏好配置。

### 应用层

- `RewriteCoordinator`：串联快捷键、微信输入读取、AI 改写和结果回填。
- `AppState`：统一管理空闲、处理中、成功和失败状态。

### 基础设施层

- `HotKeyService`：注册和处理全局快捷键。
- `AccessibilityService`：权限检查、前台应用识别、焦点元素读取和写入。
- `OpenAIService`：请求构建、响应解析、超时和错误映射。
- `KeychainService`：安全保存 OpenAI API Key。
- `SettingsStore`：保存非敏感偏好。

详细目录职责、依赖边界及数据流见 `docs/architecture.md`。

### 领域层

- `RewriteRequest`：原文、语气、场景和可选上下文。
- `RewriteResult`：优化文本及必要元数据。
- 服务协议：为 Accessibility、AI、快捷键与密钥存储提供可替换接口。

### 关键数据流

1. 全局快捷键触发 `RewriteCoordinator`。
2. 校验 Accessibility 权限及前台应用。
3. 读取微信当前焦点输入框的文本。
4. 通过 `OpenAIService` 请求优化文本。
5. 再次校验目标输入控件，避免焦点切换后误写。
6. 将结果写回输入框，不触发发送动作。
7. 菜单栏展示处理结果或可恢复错误。

### 安全与可靠性

- API Key 只存 Keychain，不进入日志、源码或版本库。
- 默认只向模型发送用户当前草稿；扩展上下文前需单独设计隐私提示。
- 网络调用设置超时、取消和结构化错误。
- 防止快捷键重复触发并发请求。
- 回填前验证微信进程和目标元素，降低误写风险。
- 不调用微信私有 API，不注入进程，不自动发送。

## 开发阶段

### 阶段 0：项目初始化

- [x] 建立 Git 功能分支工作流
- [x] 创建 README、项目说明和变更日志
- [x] 完成初步可行性分析与架构设计
- [x] 建立代码模块目录与开发规范
- [x] 用户确认初始化文档

### 阶段 1：macOS 应用骨架

- [x] 创建 SwiftUI macOS 工程
- [x] 配置 Apple Silicon 构建目标与最低系统版本
- [x] 创建菜单栏入口和基础状态菜单
- [x] 建立分层目录和核心协议占位
- [x] 添加基础单元测试
- [x] 使用命令行构建验证

第一阶段代码任务：

> 在 `feature/menu-bar` 分支创建可构建、可运行的 SwiftUI 菜单栏应用。菜单包含应用名称、当前状态“就绪”、打开设置和退出入口；暂不接入 Accessibility、快捷键与 OpenAI API。同步建立基础目录结构和最小测试目标。

验收标准：

- Xcode 能在 Apple Silicon Mac 上编译运行。
- 启动后菜单栏显示应用图标及基础菜单。
- 应用不在 Dock 中显示常规主窗口。
- 不包含 API Key 或其他敏感信息。
- 构建及基础测试通过。

### 阶段 2：Accessibility 集成（已完成）

- [x] 权限状态检测与系统设置引导
- [x] 检测微信是否为前台应用
- [x] 读取焦点输入框内容（AX 与安全剪贴板兼容层）
- [x] 写回文本并处理控件变化
- [x] 在微信 4.1.11 上完成读取手工测试

### 阶段 3：OpenAI API 集成（当前）

- [x] Keychain 安全存储 API Key（等待签名 App 人工验证）
- [ ] 实现 OpenAI 请求与响应解析
- [ ] 设计改写提示词和语气选项
- [ ] 添加超时、取消、限流和错误处理
- [ ] 使用模拟网络响应完成单元测试

### 阶段 4：完整工作流

- [x] 注册 `Command + Shift + R`
- [ ] 实现 `RewriteCoordinator`
- [ ] 添加处理中状态和重复触发保护
- [ ] 回填前二次验证目标输入框
- [ ] 完成端到端人工验收

### 阶段 5：体验与发布准备

- [ ] 设置界面与可配置快捷键
- [ ] 日志、诊断和隐私说明
- [ ] 应用签名、权限说明与打包
- [ ] 回归测试与发布检查清单

## 已完成任务

- 创建项目基础文档。
- 明确技术可行性、风险与模块边界。
- 制定功能分支、小步提交和版本记录规范。
- 修复菜单栏应用打开设置窗口时未激活到前台的问题。
- 支持根据 Bundle Identifier 检测微信是否为前台应用。
- 修复菜单栏激活导致微信前台状态误判的问题。
- 使用窗口前后顺序识别菜单栏下方的真实前台应用，并显示检测到的 Bundle Identifier。
- 配置固定开发团队签名，避免每次构建产生不同的辅助功能授权身份。
- 新增微信焦点输入框读取器、主动读取菜单入口和结构化错误提示。
- 读取完成后立即弹出结果提示，避免菜单自动关闭后反馈不可见。
- 支持在微信返回 `AX 25212` 时遍历当前窗口，回退查找可编辑输入框。
- 支持 WebKit 混合界面的多属性控件树遍历，并在失败时显示控件角色诊断。
- 支持从微信主进程及白名单内的 `WeChatAppEx` 界面进程查找输入框。
- 使用公开进程 API 发现微信 App 包内的 `WeChatAppEx Helper` 渲染进程。
- 菜单测试读取前重新激活微信并等待焦点恢复，同时请求 AppEx 手动 Accessibility 模式。
- 菜单 action 返回并完全关闭后再执行读取，避免菜单跟踪状态阻止微信恢复输入焦点。
- 新增安全剪贴板兼容读取层，在 AX 不可用时模拟复制并恢复原剪贴板全部数据。
- 完成微信 4.1.11 草稿读取及剪贴板恢复人工验证。
- 注册系统级 `Command + Shift + R`，触发现有微信草稿读取流程。
- 完成系统级快捷键在微信 4.1.11 中的人工验证。
- 新增安全剪贴板写回层和菜单测试入口，写入后恢复原剪贴板且不发送消息。
- 增强菜单关闭后的微信焦点恢复等待，并为剪贴板读取增加有限重试。
- 完成微信 4.1.11 草稿写回人工验收，确认未自动发送且原剪贴板正常恢复。
- 新增 Keychain API Key 存储、设置页安全输入以及保存和删除状态管理。
- 新增仓库级 `AGENTS.md`，固化影响分析、测试、提交和变更日志流程。

## 待开发任务

继续执行阶段 3；下一步验证签名 App 的 Keychain 保存和删除，再实现 OpenAI 请求与响应解析。
