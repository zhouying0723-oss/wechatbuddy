# WeChatBuddy 开发约束

本文件适用于整个仓库。所有开发者和自动化开发代理都必须遵守。

## 开发前

1. 阅读 `README.md`、`PROJECT.md`、`CHANGELOG.md` 和 `docs/development.md`。
2. 执行 `git status` 和 `git log -5 --oneline`，确认当前分支、工作区和最近提交。
3. 所有开发必须在对应的 `feature/*` 分支进行，不直接修改或合并到 `main`。
4. 修改代码前先分析并说明影响范围，包括涉及的模块、文件、行为、风险和验证方式。
5. 将任务拆成可独立验证的小改动，禁止积累大量修改后一次提交。

## 修改中

1. 保持 Swift、SwiftUI、Accessibility、AI、UI 和工具模块的职责边界。
2. 不提交 API Key、签名凭据、个人配置、构建产物或敏感聊天内容。
3. 应用只辅助改写输入框草稿，不自动发送微信消息。
4. 不调用微信私有 API，不注入微信进程。
5. 保留用户已有且与当前任务无关的修改。

## 修改后

每个独立功能或修复都必须按以下顺序完成：

1. 运行与影响范围匹配的构建和测试；Swift 代码至少通过 arm64 macOS 命令行构建。
2. 执行 `git diff --check`，检查差异内容。
3. 执行 `git status`，确认变更范围准确。
4. 更新 `PROJECT.md` 中的阶段和任务状态。
5. 更新 `CHANGELOG.md`，使用规定版本和日期格式记录新增、修改及修复。
6. 必要时同步更新 `README.md` 中的版本和项目状态。
7. 执行 `git add`。
8. 使用规范消息执行 `git commit`。
9. 执行 `git push`，只推送当前功能分支。
10. 再次执行 `git status`，确认工作区干净且分支与远程同步。

## 提交消息

- `feat: 新增XXX功能`
- `fix: 修复XXX问题`
- `refactor: 重构XXX模块`
- `docs: 更新XXX文档`

## 任务汇报

每次完成任务后必须汇报：

- 本次完成
- 影响范围及修改文件
- 构建与测试结果
- Git 分支和 commit id
- 当前版本
- 本次 CHANGELOG 内容
- 下一步建议和需要用户人工验证的项目

功能分支完成后等待用户确认，由用户决定何时合并到 `main`。
